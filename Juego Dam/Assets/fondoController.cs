using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class fondoController : MonoBehaviour
{
    //public Transform personaje;
    //private Renderer renderer;
    //public float ScrollingSpeed=.1f;
    // Start is called before the first frame update
    void Start()
    {
       // renderer = this.GetComponent<Renderer>();
    }

    // Update is called once per frame
    void Update()
    {
        //renderer.material.mainTextureOffset = renderer.material.mainTextureOffset + new Vector2(ScrollingSpeed, 0);
        //transform.position = new Vector2(personaje.position.x,transform.position.y);
    }
}
